const {
    MinerGate,
} = require('./MinerGate');
const {
    CryptoCompare,
} = require('./CryptoCompare');
const {
    Binance,
} = require('./Binance');

module.exports = {
    CryptoCompare,
    MinerGate,
    Binance,
};
